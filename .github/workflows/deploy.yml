name: Test Runner Connection

on:
  push:
    branches: [ "main" ]

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Echo step
        run: echo "🎉 GitHub runner is working!"
